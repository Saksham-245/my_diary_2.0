import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.sakshammathur25.MyDiary',
  appName: 'my_diary_2.0',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
